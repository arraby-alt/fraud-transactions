# fraud-transactions (iPython Environment)

## Setup Instructions

### 1. Create environment
python -m venv .venv

### 2. Activate (Windows)
.venv\Scripts\activate

### 3. Install dependencies
python -m pip install -r requirements.txt